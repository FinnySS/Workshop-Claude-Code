{
  "name": "tetris-game",
  "version": "1.0.0",
  "description": "Tetris game deployed on Cloudflare Workers",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run"
  },
  "keywords": ["tetris", "game", "cloudflare-workers"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.3.4"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
