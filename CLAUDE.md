# CLAUDE.md - คู่มือสำหรับ AI Assistant ในโครงการเกม Tetris

## 📚 สารบัญ

1. [บทสรุปผู้บริหาร](#บทสรุปผู้บริหาร)
2. [คู่มือเริ่มต้นอย่างรวดเร็ว](#คู่มือเริ่มต้นอย่างรวดเร็ว)
3. [บริบทของโครงการ](#บริบทของโครงการ)
4. [กฎความปลอดภัยที่สำคัญ](#กฎความปลอดภัยที่สำคัญ)
5. [สภาพแวดล้อมการพัฒนา](#สภาพแวดล้อมการพัฒนา)
6. [ขั้นตอนการทำงาน](#ขั้นตอนการทำงาน)
7. [การจัดการบริบทและรหัสย่อ](#การจัดการบริบทและรหัสย่อ)
8. [ข้อมูลอ้างอิงทางเทคนิค](#ข้อมูลอ้างอิงทางเทคนิค)
9. [แนวปฏิบัติในการพัฒนา](#แนวปฏิบัติในการพัฒนา)
10. [บทเรียนที่ได้รับ](#บทเรียนที่ได้รับ)
11. [การแก้ไขปัญหา](#การแก้ไขปัญหา)
12. [ภาคผนวก](#ภาคผนวก)

## บทสรุปผู้บริหาร

เอกสารนี้เป็นแนวทางสำหรับ AI Assistant ที่ทำงานในโครงการเกม Tetris บน Cloudflare Workers โดยกำหนดขั้นตอนการทำงานที่ปลอดภัย มีประสิทธิภาพ และมีการบันทึกไว้อย่างดี

### ความรับผิดชอบหลัก
- การพัฒนาและปรับปรุงโค้ดเกม
- การทดสอบและประกันคุณภาพ
- การทำเอกสารและการสรุปผลการทำงาน (Session Retrospectives)
- การปฏิบัติตามขั้นตอนการพัฒนาที่ปลอดภัย
- การรักษาบริบทและประวัติของโครงการ

### ข้อมูลอ้างอิงด่วน - รหัสย่อ (Short Codes)

#### ขั้นตอนการวางแผนและบริบท (Core Pattern)
- `ccc` - สร้าง Context Issue และย่อบทสนทนา (Compact)
- `nnn` - วางแผนอัจฉริยะ: รัน `ccc` อัตโนมัติถ้าไม่มี Context → สร้างแผนการทำงานโดยละเอียด
- `gogogo` - ดำเนินการตามแผนล่าสุดทีละขั้นตอน
- `lll` - แสดงสถานะโครงการ (Issues, PRs, Commits) ✅

#### การจัดการโครงการ
- `rrr` - สร้างบันทึกสรุปผลการทำงาน (Retrospective) โดยละเอียด

## คู่มือเริ่มต้นอย่างรวดเร็ว

### สิ่งที่ต้องมีเบื้องต้น
```bash
# ตรวจสอบเครื่องมือที่จำเป็น
python3 --version   # สำหรับ local dev server
node --version      # สำหรับ Cloudflare Wrangler
git --version       # Version control
gh --version        # GitHub CLI
```

### การตั้งค่าเริ่มต้น
```bash
# 1. Clone repository (ถ้ายังไม่ได้ทำ)
git clone https://github.com/FinnySS/Workshop-Claude-Code
cd Workshop-Claude-Code

# 2. ติดตั้ง dependencies
npm install

# 3. ทดสอบ local
npm run dev

# 4. Deploy ไปยัง Cloudflare
npm run deploy
```

### งานแรก
1. รัน `lll` เพื่อดูสถานะโครงการปัจจุบัน
2. รัน `nnn` เพื่อวิเคราะห์ Issue ล่าสุดและสร้างแผน
3. ใช้ `gogogo` เพื่อดำเนินการตามแผน

## บริบทของโครงการ

### ภาพรวมโครงการ

โครงการนี้เป็นเกม Tetris แบบคลาสสิกที่ทำงานบนเบราว์เซอร์ โดย Deploy บน Cloudflare Workers เป้าหมายคือการสร้างเกมสไตล์อาร์เคดที่มีประสิทธิภาพ สวยงาม และเล่นได้ลื่นไหล

### สถาปัตยกรรม (Architecture)

- **Frontend**: HTML5 Canvas, CSS3, Vanilla JavaScript (ES6+)
- **Game Engine**: `requestAnimationFrame` สำหรับ Game Loop
- **State Management**: Object-based state (คะแนน, เลเวล, กระดาน)
- **Infrastructure**: Cloudflare Workers (Static Assets)
- **Development**: GitHub Codespaces

### ฟีเจอร์ปัจจุบัน

- **Core Gameplay:**
  - เรนเดอร์ Tetromino 7 รูปร่าง (I, L, J, O, Z, S, T)
  - การเคลื่อนที่ (ซ้าย, ขวา, หมุน, Soft Drop)
  - การตรวจจับการชนแบบ Matrix-based
  - การเคลียร์แถวและคิดคะแนน
  - ระบบเลเวลและความเร็ว

- **UI/UX:**
  - แสดงคะแนน, เลเวล, จำนวนแถว
  - ปุ่ม Start/Pause
  - สไตล์ภาพแบบ Neon/Arcade
  - การควบคุมด้วยแป้นพิมพ์

## 🔴 กฎความปลอดภัยที่สำคัญ

### การใช้ Repository
- **ห้ามสร้าง Issues/PRs บน Upstream โดยเด็ดขาด**

### การใช้คำสั่ง
- **ห้ามใช้ `-f` หรือ `--force` flags กับคำสั่งใดๆ**
- ใช้คำสั่งที่ปลอดภัยและไม่ทำลายข้อมูลเสมอ
- หากคำสั่งต้องการการยืนยัน ให้จัดการอย่างเหมาะสมโดยไม่บังคับ

### การจัดการ Git
- ห้ามใช้ `git push --force` หรือ `git push -f`
- ห้ามใช้ `git checkout -f`
- ห้ามใช้ `git clean -f`
- **⚠️ ห้าม MERGE PULL REQUESTS โดยไม่ได้รับอนุญาตจากผู้ใช้อย่างชัดเจน**
- **ห้ามใช้ `gh pr merge` เว้นแต่ได้รับคำสั่งจากผู้ใช้**
- รอให้ผู้ใช้ตรวจสอบและอนุมัติก่อนทำการ Merge เสมอ

### การจัดการไฟล์
- ห้ามใช้ `rm -rf` - ให้ใช้ `rm -i` เพื่อยืนยันแบบ Interactive
- ยืนยันก่อนลบไฟล์เสมอ
- ใช้การดำเนินการไฟล์ที่ปลอดภัยและสามารถกู้คืนได้

### Package Manager Operations
- ห้ามใช้ `npm install --force`
- ห้ามใช้ `npm update` โดยไม่ระบุ packages
- ตรวจสอบการเปลี่ยนแปลง lockfile ก่อน commit เสมอ

### คำแนะนำความปลอดภัยทั่วไป
- ให้ความสำคัญกับความปลอดภัยและความสามารถในการย้อนกลับ
- ขอการยืนยันเมื่อทำการกระทำที่อาจทำลายข้อมูล
- อธิบายผลกระทบของคำสั่งก่อนที่จะดำเนินการ
- ใช้ตัวเลือก Verbose เพื่อแสดงสิ่งที่คำสั่งกำลังทำ

## สภาพแวดล้อมการพัฒนา

### ตัวแปรสภาพแวดล้อม

#### สำหรับ Codespaces
```
CODESPACE_NAME=                                    # ใช้สำหรับ URL
GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN=          # ใช้สำหรับ URL
```

#### สำหรับ Cloudflare Workers
```
# ไม่จำเป็นต้องมี environment variables สำหรับ static assets
# wrangler.jsonc จัดการ configuration ทั้งหมด
```

## ขั้นตอนการทำงาน

### วินัยในการทดสอบ

#### รายการตรวจสอบการทดสอบด้วยตนเอง
ก่อนที่จะ Push การเปลี่ยนแปลงใดๆ:

- [ ] ทดสอบ local: `npm run dev` หรือ `python3 -m http.server 3000`
- [ ] ตรวจสอบว่าเกมโหลดได้
- [ ] ทดสอบการควบคุม (ปุ่มลูกศร: ซ้าย, ขวา, ขึ้น-หมุน, ลง-drop)
- [ ] ตรวจสอบบั๊กการชน (ชิ้นส่วนทะลุกำแพง/พื้น)
- [ ] ตรวจสอบการเคลียร์แถวและการอัปเดตคะแนน
- [ ] ตรวจสอบ Browser Console เพื่อหา JavaScript Errors
- [ ] ทดสอบ Game Over และ Restart

### GitHub Workflow

#### การสร้าง Issues
เมื่อเริ่มงานใหม่หรือแก้บั๊ก:
```bash
# 1. อัปเดต main branch
git checkout main && git pull

# 2. สร้าง detailed issue
gh issue create --title "feat: คำอธิบายสั้นๆ" --body "$(cat <<'EOF'
## ภาพรวม
คำอธิบายสั้นๆ ของฟีเจอร์/บั๊ก

## สถานะปัจจุบัน
สิ่งที่มีอยู่ตอนนี้

## โซลูชันที่เสนอ
สิ่งที่ควรทำ

## รายละเอียดทางเทคนิค
- Components ที่เกี่ยวข้อง
- แนวทางการทำงาน

## Acceptance Criteria
- [ ] เกณฑ์ที่ทดสอบได้
- [ ] ข้อกำหนดประสิทธิภาพ
- [ ] ข้อกำหนด UI/UX
EOF
)"
```

#### ขั้นตอนการพัฒนามาตรฐาน
```bash
# 1. สร้าง branch จาก issue
git checkout -b feat/issue-number-description

# 2. แก้ไข
# ... implement feature ...

# 3. ทดสอบอย่างละเอียด
# ใช้รายการตรวจสอบด้านบน

# 4. Commit ด้วยข้อความที่อธิบายชัดเจน
git add -A
git commit -m "feat: คำอธิบายสั้นๆ

- What: การเปลี่ยนแปลงเฉพาะ
- Why: แรงจูงใจ
- Impact: สิ่งที่ได้รับผลกระทบ

Closes #issue-number"

# 5. Push และสร้าง PR
git push -u origin branch-name
gh pr create --title "เหมือนกับ commit" --body "Fixes #issue_number"

# 6. ⚠️ สำคัญ: ห้าม MERGE PRs ด้วยตัวเอง
# ห้าม: gh pr merge
# ให้ PR link กับผู้ใช้เท่านั้น
# รอคำสั่งจากผู้ใช้เพื่อ merge
```

## การจัดการบริบทและรหัสย่อ

### ทำไมต้องใช้รูปแบบสอง Issues?
ขั้นตอน `ccc` → `nnn` ใช้รูปแบบสอง Issues:
1. **Context Issues** (`ccc`): เก็บสถานะและบริบทของ Session
2. **Task Issues** (`nnn`): มีแผนการทำงานจริง

การแยกนี้ช่วยให้มีความชัดเจนระหว่างการเก็บข้อมูลบริบทและงานที่ต้องทำ ทำให้การจัดการดีขึ้นและการติดตามงานชัดเจนขึ้น

### รหัสย่อหลัก (Core Short Codes)

#### `ccc` - สร้าง Context และ Compact
**วัตถุประสงค์**: บันทึกสถานะ Session ปัจจุบันและบริบท

**ขั้นตอน:**
1. **รวบรวมข้อมูล**: `git status --porcelain`, `git log --oneline -5`
2. **สร้าง GitHub Context Issue**: จับภาพสถานะปัจจุบัน, ไฟล์ที่เปลี่ยน, การค้นพบสำคัญ
3. **Compact Conversation**: `/compact`

#### `nnn` - การวางแผนงานถัดไป
**วัตถุประสงค์**: สร้างแผนการทำงานที่ครอบคลุม **ไม่เขียนโค้ด** - วิเคราะห์และวางแผนเท่านั้น

**ขั้นตอน:**
1. **ตรวจสอบ Context ล่าสุด**: ถ้าไม่มี ให้รัน `ccc` ก่อน
2. **รวบรวม Context**: วิเคราะห์ Context Issue ล่าสุด หรือ Issue ที่ระบุ (`nnn #123`)
3. **วิเคราะห์เชิงลึก**: อ่าน Context, วิเคราะห์ Codebase, ค้นคว้ารูปแบบ
4. **สร้าง Plan Issue**: สรุปปัญหา, การวิจัย, โซลูชัน, ขั้นตอน, ความเสี่ยง
5. **ให้สรุป**: สรุปการวิเคราะห์และหมายเลข Issue

#### `lll` - แสดงสถานะโครงการ ✅
**วัตถุประสงค์**: ดูภาพรวมของโครงการ

**ขั้นตอน:**
- รันคำสั่ง `gh` และ `git` แบบขนานเพื่อดูสถานะ
- แสดง Issues ที่เปิดอยู่, PRs ล่าสุด, Commits ล่าสุด

#### `rrr` - Retrospective
**วัตถุประสงค์**: บันทึกกิจกรรม การเรียนรู้ และผลลัพธ์ของ Session

**⚠️ สำคัญมาก**: ส่วน AI Diary และ Honest Feedback เป็นส่วนที่บังคับ

**ขั้นตอน:**
1. **รวบรวมข้อมูล Session**: `git diff`, `git log`, timestamps
2. **สร้างเอกสาร Retrospective**: ใช้ Template ใน `retrospectives/`
3. **ตรวจสอบความสมบูรณ์**: ใช้ Validation Checklist
4. **อัปเดต CLAUDE.md**: เพิ่มบทเรียนใหม่ที่ด้านล่าง
5. **Link กับ GitHub**: Commit และ comment ใน Issue/PR

**Template Retrospective:**
```bash
# รับวันที่และเวลา
SESSION_DATE=$(date +"%Y-%m-%d")
END_TIME_UTC=$(date -u +"%H:%M")
END_TIME_LOCAL=$(TZ='Asia/Bangkok' date +"%H:%M")

# สร้างไดเรกทอรี
mkdir -p retrospectives/$(date +%Y/%m)

# สร้างไฟล์
cat > retrospectives/$(date +%Y/%m)/${SESSION_DATE}_${END_TIME_UTC//:/-}_retrospective.md << 'EOF'
# Session Retrospective

**Session Date**: [DATE]
**Start Time**: [START] GMT+7 ([START] UTC)
**End Time**: [END] GMT+7 ([END] UTC)
**Duration**: ~X นาที
**Primary Focus**: คำอธิบายสั้นๆ
**Session Type**: [Feature Development | Bug Fix | Research | Refactoring]
**Current Issue**: #XXX
**Last PR**: #XXX

## สรุป Session
[ภาพรวม 2-3 ประโยค]

## Timeline
- HH:MM - เริ่ม Session
- HH:MM - [เหตุการณ์]
- HH:MM - เสร็จสิ้น

## รายละเอียดทางเทคนิค

### ไฟล์ที่แก้ไข
```
[git diff --name-only output]
```

### การเปลี่ยนแปลงโค้ดสำคัญ
- [รายละเอียด]

### การตัดสินใจเชิงสถาปัตยกรรม
- [การตัดสินใจและเหตุผล]

## 📝 AI Diary (บังคับ - ห้ามข้าม)
**⚠️ บังคับ: ส่วนนี้ให้บริบทสำคัญสำหรับ Session ในอนาคต**
[บรรยายประสบการณ์แบบบุคคลที่หนึ่ง:
- ความเข้าใจเริ่มต้น
- วิธีที่แนวทางพัฒนาไป
- ช่วงเวลาสับสนหรือกระจ่าง
- การตัดสินใจและเหตุผล
- สิ่งที่ประหลาดใจ
- กระบวนการคิด]

## สิ่งที่ดี
- [ความสำเร็จ]

## สิ่งที่ควรปรับปรุง
- [พื้นที่]

## อุปสรรคและแนวทางแก้ไข
- **อุปสรรค**: [คำอธิบาย]
  **แนวทางแก้ไข**: [วิธีแก้]

## 💭 Honest Feedback (บังคับ - ห้ามข้าม)
**⚠️ บังคับ: ช่วยให้มีการปรับปรุงอย่างต่อเนื่อง**
[ประเมินอย่างตรงไปตรงมา:
- ประสิทธิภาพ Session
- ประสิทธิภาพเครื่องมือ
- ความชัดเจนในการสื่อสาร
- ประสิทธิภาพกระบวนการ
- สิ่งที่หงุดหงิด
- สิ่งที่พอใจ
- ข้อเสนอแนะ]

## บทเรียนที่ได้รับ
- **Pattern**: [คำอธิบาย] - [ความสำคัญ]
- **ข้อผิดพลาด**: [เกิดอะไร] - [วิธีหลีกเลี่ยง]
- **การค้นพบ**: [เรียนรู้อะไร] - [วิธีประยุกต์]

## ขั้นตอนต่อไป
- [ ] งานเร่งด่วน
- [ ] งานติดตาม
- [ ] การพิจารณาในอนาคต

## ทรัพยากรที่เกี่ยวข้อง
- Issue: #XXX
- PR: #XXX

## ✅ Retrospective Validation Checklist
- [ ] AI Diary มีบรรยายโดยละเอียด (ไม่ใช่ตัวยึด)
- [ ] Honest Feedback มีการประเมินตรงไปตรงมา (ไม่ใช่ตัวยึด)
- [ ] สรุป Session ชัดเจน
- [ ] Timeline มีเวลาและเหตุการณ์จริง
- [ ] รายละเอียดทางเทคนิคถูกต้อง
- [ ] บทเรียนมีข้อมูลที่นำไปใช้ได้
- [ ] ขั้นตอนต่อไปเฉพาะเจาะจง

⚠️ **สำคัญ**: Retrospective ที่ไม่มี AI Diary และ Honest Feedback ไม่สมบูรณ์
EOF
```

**หมายเหตุเขตเวลา**:
- **เขตเวลาหลัก: GMT+7 (กรุงเทพฯ)** - แสดง GMT+7 เป็นอันดับแรกเสมอ
- UTC รวมไว้เพื่ออ้างอิง (ในวงเล็บ)
- ชื่อไฟล์อาจใช้ UTC เพื่อความสอดคล้อง

#### `gogogo` - ดำเนินการตามแผน
**วัตถุประสงค์**: Execute implementation ตามแผน

**ขั้นตอน:**
1. **หา Implementation Issue**: ค้นหา `plan:` Issue ล่าสุด
2. **Execute**: ทำตามแผนทีละขั้นตอน
3. **ทดสอบและตรวจสอบ**: รันการทดสอบ
4. **Commit และ Push**: Commit, Push, สร้าง/อัปเดต PR

## ข้อมูลอ้างอิงทางเทคนิค

### รายละเอียดตรรกะเกม

#### The Arena (กระดานเกม)
- Matrix ขนาด 12x20
- 0 = ช่องว่าง, 1-7 = สี Tetromino

#### Collision Detection
```javascript
function collide(arena, player) {
  const [m, o] = [player.matrix, player.pos];
  for (let y = 0; y < m.length; ++y) {
    for (let x = 0; x < m[y].length; ++x) {
      if (m[y][x] !== 0 &&
          (arena[y + o.y] && arena[y + o.y][x + o.x]) !== 0) {
        return true;
      }
    }
  }
  return false;
}
```

#### Rotation Logic
- Transpose matrix
- Reverse rows (สำหรับหมุนตามเข็มนาฬิกา)
- Wall kick detection

### Available Tools

#### Version Control
```bash
git status          # ดูสถานะ
git add -A          # เพิ่มไฟล์ทั้งหมด
git commit -m "msg" # Commit
git push            # Push

gh issue create     # สร้าง Issue
gh pr create        # สร้าง PR
```

#### Development
```bash
npm install         # ติดตั้ง dependencies
npm run dev         # ทดสอบ local
npm run deploy      # Deploy ไป Cloudflare

# หรือ Python (สำหรับ local dev)
python3 -m http.server 3000
```

## แนวปฏิบัติในการพัฒนา

### มาตรฐานโค้ด

- **JavaScript**: ใช้ฟีเจอร์ ES6+ (const/let, arrow functions)
- **Formatting**: การย่อหน้าสม่ำเสมอ (4 spaces)
- **Comments**: อธิบาย *ทำไม* (why) ไม่ใช่ *ทำอะไร* (what)
- เขียนโค้ดที่อธิบายตัวเองชัดเจน
- หลีกเลี่ยง `any` หรือ weak types

### รูปแบบ Git Commit
```
[type]: [คำอธิบายสั้นๆ]

- What: [การเปลี่ยนแปลง]
- Why: [แรงจูงใจ]
- Impact: [พื้นที่ที่ได้รับผลกระทบ]

Closes #[issue-number]
```
**Types**: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`

### Error Handling Patterns
- ใช้ `try/catch` สำหรับการดำเนินการที่อาจล้มเหลว
- ให้ข้อความข้อผิดพลาดที่อธิบายชัดเจน
- ทำ Graceful Fallbacks ใน UI
- ใช้ Custom Error Types เมื่อเหมาะสม

## บทเรียนที่ได้รับ

### Planning & Architecture Patterns
- **Pattern**: ใช้ Parallel Agents สำหรับวิเคราะห์ด้านต่างๆ
- **Anti-Pattern**: สร้างแผน Monolithic ที่ทำทุกอย่างพร้อมกัน
- **Pattern**: ถามว่า "ขั้นตอนแรกที่เล็กที่สุดคืออะไร?"
- **Pattern**: ชิ้นงาน 1 ชั่วโมงเหมาะสมที่สุดสำหรับ focus และเห็นความก้าวหน้า

### ข้อผิดพลาดทั่วไปที่ควรหลีกเลี่ยง
- **สร้างแผนครอบคลุมเกินไป** - แบ่งเป็นเฟส 1 ชั่วโมงแทน
- **ทำทุกอย่างพร้อมกัน** - เริ่มด้วย MVP, ทดสอบ, ขยาย
- **ข้าม AI Diary และ Honest Feedback** - ส่วนนี้ให้บริบทสำคัญ
- ลืมอัปเดต lockfile หลังเปลี่ยน dependencies
- ไม่ตรวจสอบ build logs สำหรับคำเตือน

### เคล็ดลับที่มีประโยชน์
- **Parallel Agents**: วิเคราะห์หลายด้านพร้อมกันเร่งการวางแผน
- **ccc → nnn workflow**: จับภาพบริบทแล้ววางแผนที่มีโฟกัส
- **Phase Markers**: ใช้ "Phase 1:", "Phase 2:" ติดตามความก้าวหน้า

### สิ่งที่พบเฉพาะโครงการ (Tetris)

#### Cloudflare Workers Deployment
- **ปัญหา**: Workers Sites deprecated และ error "Missing entry-point"
- **วิธีแก้**: ใช้ `wrangler.jsonc` กับ `assets.directory` แทน
- **Pattern**: Static assets deployment ไม่ต้องใช้ worker code

#### Canvas Coordinate System
- **เคล็ดลับ**: `context.scale(20, 20)` ทำให้ตรรกะทำงานใน "หน่วยตาราง" (1x1) แต่เรนเดอร์เป็นพิกเซล (20x20)
- ช่วยให้คำนวณง่ายขึ้น

#### Background Process Persistence (Codespaces)
- **ปัญหา**: `python3 -m http.server &` ถูกปิดเมื่อ CLI tool ทำงานเสร็จ
- **วิธีแก้**: `setsid python3 -m http.server 3000 > server.log 2>&1 &`
- แยก Process ออกจาก Session

### ความชอบของผู้ใช้
- **ชอบขอบเขตที่จัดการได้** - งานที่ทำเสร็จได้ภายใน 1 ชั่วโมง
- **ให้คุณค่ากับแนวทางแบ่งเฟส** - แบ่งงานใหญ่ออกเป็นเฟสย่อย
- **ชื่นชมรูปแบบ workflow** - ชอบใช้ "ccc nnn gh flow"
- **เขตเวลา GMT+7** (กรุงเทพฯ/เอเชีย)

## การแก้ไขปัญหา

### ปัญหาทั่วไป

#### Build Failures (Cloudflare)
```bash
# ตรวจสอบ wrangler.jsonc
cat wrangler.jsonc

# ตรวจสอบ assets directory
ls -la public/

# ทดสอบ local
npm run dev
```

#### Server ไม่ทำงาน (Local Dev)
**อาการ**: Connection refused
**วิธีแก้**:
```bash
# 1. ตรวจสอบ process
ps aux | grep python

# 2. ตรวจสอบ logs
cat server.log

# 3. Restart
pkill -f http.server
setsid python3 -m http.server 3000 > server.log 2>&1 &
```

#### Game Logic Bugs
**อาการ**: ชิ้นส่วนทะลุกำแพง, การหมุนผิด
**วิธีแก้**:
1. ตรวจสอบ `collide()` function
2. ตรวจสอบ `rotate()` logic และ wall kicks
3. ดู console logs สำหรับ errors
4. ทดสอบแต่ละ tetromino shape

## ภาคผนวก

### A. อภิธานศัพท์
- **Tetromino**: รูปทรง 7 แบบ (I, J, L, O, S, T, Z)
- **Arena**: Matrix กระดานเกม
- **ccc, nnn, rrr, lll, gogogo**: Short codes สำหรับ workflow
- **Cloudflare Workers**: Platform สำหรับ deploy
- **wrangler**: CLI tool สำหรับ Cloudflare

### B. Quick Command Reference
```bash
# Development
npm install              # ติดตั้ง dependencies
npm run dev              # ทดสอบ local
npm run deploy           # Deploy
python3 -m http.server   # Local server (alternative)

# Git
git status               # ดูสถานะ
git add -A               # เพิ่มไฟล์ทั้งหมด
git commit -m "msg"      # Commit
git push                 # Push

# GitHub
gh issue create          # สร้าง Issue
gh pr create             # สร้าง PR
gh issue list            # ดู Issues
gh pr list               # ดู PRs
```

### C. Environment Checklist
- [ ] Node.js v22.16.0+ installed
- [ ] Python 3 installed (สำหรับ local dev)
- [ ] Git configured
- [ ] GitHub CLI configured
- [ ] npm dependencies installed
- [ ] wrangler.jsonc configured
- [ ] public/ directory มีไฟล์เกม

---

**Last Updated**: 2025-12-26
**Version**: 1.0.0
**Project**: Tetris Game on Cloudflare Workers
**Repository**: https://github.com/FinnySS/Workshop-Claude-Code
